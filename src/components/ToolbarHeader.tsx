import React from 'react';
import { Header } from 'react-native-elements';

const GD_ToolbarHeader = () => {
  return (
    <Header placement="left" leftComponent={{ icon: 'menu', color: '#fff' }} />
  );
};

export default GD_ToolbarHeader;
